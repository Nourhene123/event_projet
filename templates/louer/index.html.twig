{% extends 'base.html.twig' %}

{% block title %}Louer index{% endblock %}

{% block body %}
    <h1>Louer index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for louer in louers %}
            <tr>
                <td>{{ louer.id }}</td>
                <td>
                    <a href="{{ path('app_louer_show', {'id': louer.id}) }}">show</a>
                    <a href="{{ path('app_louer_edit', {'id': louer.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_louer_new') }}">Create new</a>
{% endblock %}
