<form method="post" action="{{ path('app_louer_delete', {'id': louer.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ louer.id) }}">
    <button class="btn">Delete</button>
</form>
